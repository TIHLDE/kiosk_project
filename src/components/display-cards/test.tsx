"use client";

import CardWrapper from "./wrapper";

export default function TestCard() {
    return (
        <CardWrapper className="flex items-center justify-center">
            <div>Her tester vi mann</div>
        </CardWrapper>
    );
};